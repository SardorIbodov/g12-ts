const [first, second] = ["frontend", "backend"];
const logMessage = (message: string): string => message;
let some = "web";

let someText: string = "Webbrain";
// someText = 2023;
console.log(someText);
